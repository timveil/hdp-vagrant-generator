{
    "blueprint" : "{{blueprintName}}",
    "default_password" : "{{kdcPassword}}",
{{#arguments.kerberosEnabled}}
    "credentials" : [
        {
            "alias" : "kdc.admin.credential",
            "principal" : "{{kdcAdmin}}",
            "key" : "{{kdcPassword}}",
            "type" : "TEMPORARY"
        }
    ],
    "security" : {
        "type" : "KERBEROS"
    },
{{/arguments.kerberosEnabled}}
    "host_groups" :[
        {
            "name" : "host_group_1",
            "hosts" : [
                {
                    "fqdn" : "{{arguments.fqdn}}"
                }
            ]
        }
    ]
}
